import { CalloutTriggerDirective } from './callout-trigger.directive';

describe('CalloutTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new CalloutTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
